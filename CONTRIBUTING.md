# Contributing to Taghag

1. Keep it MP3/FLAC-focused and clean-room.
2. Please do not reintroduce legacy Tagslut schemas.
3. Keep database migrations strictly source-controlled under `supabase/migrations`.
4. Ensure the `tools/taghag_import` unit tests pass (`cd tools && pytest`).

Please submit issues or PRs for any improvements.
