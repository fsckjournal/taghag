.DS_Store
.env
.env.local
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.pytest_cache/
.mypy_cache/
node_modules/
dist/
coverage/
receipts/
artifacts/

# Temp and Scratch Files
supabase/.temp/
*.csv
*.m3u
*.m3u8
*.log
tools/out.txt

# Postman local directories and runtime data
.postman/
postman/

# Delivery overlays and installers
.taghag-delivery/
taghag-overlay-installation-report.txt
*.zip

# Local caches
tools/tags_cache.json
archive/raw_dumps/172_utf8.txt

# Local raw capture dumps
archive/raw_dumps/*.har

# Forensic DB snapshots — large personal databases (~2.4 GB), never commit
tools/forensic/snapshots/

# Rendered mix audio / scratch outputs
*.flac
out.txt

# Transcripts, chatlogs, conversation/data dumps — NEVER commit (may carry creds).
# Broad ignore of dump file types; essential tracked configs re-included below.
chatlogs/
*.jsonl
*.txt
*.json
Building Automated Beatmatched Mixer*.md
THE_MASTER_RECORD.md

# Re-include essential tracked config / build / data files that match the above:
!.vscode/settings.json
!tools/neighborhood.json
!tools/taghag_import/genre_rules.json
!tools/tests/fixtures/iwebdj_payloads.json
!web/package-lock.json
!web/package.json
!web/tsconfig.json
!archive/raw_dumps/172.txt
!pool_metadata_backup.jsonl
!.mcp.json
!server/package.json
!server/package-lock.json

# Committed sample input set for the mixer (intentional binaries — see its README)
!tools/mixslice/samples/
!tools/mixslice/samples/**

# Root-level debug/plot artifacts (matplotlib output, pasted screenshots)
/*.png

# Echo Nest automix payload corpus — 22,808 files, ~27GB, derived cache, never commit
automix_payloads/

# spotify_id<->ISRC bridge outputs — regenerable via build_spotify_isrc_bridge.py
tools/cue_heuristic/spotify_isrc_bridge.jsonl
tools/cue_heuristic/track_alias_spotify_id.sql
